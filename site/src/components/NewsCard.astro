---
import PriorityBadge from "./PriorityBadge.astro";
import { genreLabels, sourceKey, sourceLabel, type Article, type Genre } from "../lib/articles";
import { hasTagPage, tagPathSegment } from "../lib/stats";

interface Props {
  article: Article;
}

const { article } = Astro.props;
const allTags = Array.from(new Set([...article.entity_tags, ...article.title_tags].filter(Boolean)));
const relativeFormatter = new Intl.RelativeTimeFormat("ja", { numeric: "auto" });
const timestamp = new Date(article.timestamp);
const diffHours = Math.round((timestamp.getTime() - Date.now()) / 3_600_000);
const relativeTime =
  Number.isNaN(timestamp.getTime())
    ? ""
    : Math.abs(diffHours) < 24
      ? relativeFormatter.format(diffHours, "hour")
      : relativeFormatter.format(Math.round(diffHours / 24), "day");
const searchBlob = [
  article.category_name,
  ...article.entity_tags,
  ...article.title_tags,
  article.summary,
  article.author,
]
  .filter(Boolean)
  .join(" ")
  .toLowerCase();
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const entities = article.entity_tags.slice(0, 3).map((tag) => ({
  tag,
  href: hasTagPage(tag) ? `${base}/tags/${tagPathSegment(tag)}/` : null,
}));
const imageSrc = article.image_url ? `${base}${article.image_url}` : null;
const isUrgent = article.final_priority === "S" || article.final_priority === "A";
const source = { key: sourceKey(article), label: sourceLabel(article) };
const articleId = article.url;
const genreAccent: Record<Genre, { start: string; end: string; glow: string; border: string }> = {
  games: {
    start: "var(--color-games-start)",
    end: "var(--color-games-end)",
    glow: "var(--glow-games)",
    border: "rgba(6, 182, 212, 0.42)",
  },
  anime: {
    start: "var(--color-anime-start)",
    end: "var(--color-anime-end)",
    glow: "var(--glow-anime)",
    border: "rgba(236, 72, 153, 0.42)",
  },
  disney: {
    start: "var(--color-disney-start)",
    end: "var(--color-disney-end)",
    glow: "var(--glow-disney)",
    border: "rgba(251, 191, 36, 0.52)",
  },
};
const accent = genreAccent[article.genre];
const cardStyle = `--card-accent: ${accent.start}; --card-accent-end: ${accent.end}; --card-glow: ${accent.glow}; --card-border-hover: ${accent.border};`;
const genreBadgeStyle = `background: linear-gradient(135deg, ${accent.start}, ${accent.end});`;
const fallbackStyle = `background: radial-gradient(circle at 28% 18%, rgba(255,255,255,0.38), transparent 10rem), linear-gradient(135deg, ${accent.start}, ${accent.end});`;
---

<article
  data-article-card
  data-article-id={articleId}
  data-timestamp={article.timestamp}
  data-priority={article.final_priority}
  data-search-blob={searchBlob}
  data-tags={JSON.stringify(allTags)}
  data-source={source.key}
  data-source-label={source.label}
  tabindex="-1"
  style={cardStyle}
  class="glass-card group relative flex h-full flex-col overflow-hidden transition-opacity"
>
  <span
    class="pointer-events-none absolute left-3 top-3 z-10 hidden rounded-full bg-slate-950/80 px-2.5 py-1 text-xs font-bold text-white shadow-sm backdrop-blur-md dark:bg-white/90 dark:text-slate-950"
    data-read-label
  >
    既読
  </span>
  <div class="absolute right-3 top-3 z-20 flex items-center gap-1.5">
    <button
      type="button"
      class="glass-icon-button h-9 w-9 text-base leading-none hover:text-rose-500"
      data-hide-toggle
      aria-label="この記事を不要として隠す"
      aria-pressed="false"
      title="不要としてマーク"
    >
      🚫
    </button>
    <button
      type="button"
      class="glass-icon-button h-9 w-9 text-lg leading-none hover:text-[var(--card-accent)]"
      data-favorite-toggle
      aria-label="お気に入りに追加"
      aria-pressed="false"
    >
      ☆
    </button>
  </div>
  <div class="aspect-[4/3] overflow-hidden bg-slate-200/70 dark:bg-slate-900/70">
    <a href={article.url} target="_blank" rel="noreferrer" data-article-link class="block h-full">
      {
        imageSrc ? (
          <img
            src={imageSrc}
            alt=""
            class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.035]"
            loading="lazy"
          />
        ) : (
          <div class="flex h-full w-full flex-col items-center justify-center gap-2" style={fallbackStyle}>
            {article.favicon_url && (
              <img
                src={article.favicon_url}
                alt=""
                class="h-12 w-12 rounded-2xl bg-white/90 p-1.5 shadow"
                loading="lazy"
              />
            )}
            <span class="text-sm font-semibold text-white/90">{genreLabels[article.genre]}</span>
            {article.domain && (
              <span class="text-xs text-white/70">{article.domain}</span>
            )}
          </div>
        )
      }
    </a>
  </div>
  <div class="flex flex-1 flex-col gap-3 border-t border-[var(--glass-hairline)] bg-white/48 p-4 dark:bg-slate-950/28">
    <a href={article.url} target="_blank" rel="noreferrer" data-article-link class="contents">
      <div class="flex flex-wrap items-center gap-2">
        <PriorityBadge priority={article.final_priority} />
        <span class="rounded-full px-2.5 py-1 text-xs font-bold text-white shadow-sm" style={genreBadgeStyle}>
          {genreLabels[article.genre]}
        </span>
        {
          article.flags.speed === "速報" && (
            <span class="rounded-full bg-cyan-500 px-2.5 py-1 text-xs font-bold text-white shadow-sm">速報</span>
          )
        }
        {
          article.flags.spoiler !== "なし" && (
            <span class="rounded-full bg-slate-950 px-2.5 py-1 text-xs font-bold text-white shadow-sm dark:bg-white dark:text-slate-950">ネタバレ注意</span>
          )
        }
      </div>
      <h3 class="news-card-title text-lg font-black leading-snug">
        {article.category_name}
      </h3>
      <p class="line-clamp-2 text-sm leading-6 section-copy">
        {article.summary || "概要はまだありません。"}
      </p>
    </a>
    {
      entities.length > 0 && (
        <div class="flex flex-wrap gap-1.5">
          {entities.map((item) =>
            item.href ? (
              <a href={item.href} class="entity-tag rounded-full px-2.5 py-1 text-xs font-medium">
                {item.tag}
              </a>
            ) : (
              <span class="entity-tag rounded-full px-2.5 py-1 text-xs font-medium">
                {item.tag}
              </span>
            ),
          )}
        </div>
      )
    }
    <a href={article.url} target="_blank" rel="noreferrer" data-article-link class="mt-auto block">
      <div class="flex items-center justify-between gap-3 border-t border-[var(--glass-hairline)] pt-3 text-xs section-kicker">
        <span class="min-w-0 truncate">{article.author || "unknown"}</span>
        <time
          datetime={article.timestamp}
          class:list={[
            "shrink-0",
            isUrgent && "font-bold text-rose-600 dark:text-rose-300",
          ]}
        >
          {relativeTime}
        </time>
      </div>
    </a>
  </div>
</article>
