---
interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const pathWithBase = (path: string) => `${base}${path}`;
const links = [
  { href: pathWithBase("/"), label: "ホーム" },
  { href: pathWithBase("/games/"), label: "ゲーム" },
  { href: pathWithBase("/anime/"), label: "アニメ" },
  { href: pathWithBase("/disney/"), label: "Disney" },
  { href: pathWithBase("/trends/"), label: "トレンド" },
  { href: pathWithBase("/favorites/"), label: "お気に入り" },
  { href: pathWithBase("/newspaper/"), label: "新聞" },
];

const normalize = (value: string) => (value.endsWith("/") ? value : `${value}/`);
---

<nav class="flex gap-2 overflow-x-auto border-b border-stone-400 pb-2 dark:border-stone-700" aria-label="カテゴリ">
  {
    links.map((link) => {
      const active = normalize(currentPath) === normalize(link.href);
      return (
        <a
          href={link.href}
          class:list={[
            "whitespace-nowrap rounded-sm px-3 py-2 text-sm font-medium",
            active
              ? "bg-[var(--accent)] text-white dark:bg-stone-100 dark:text-stone-950"
              : "text-stone-700 hover:bg-stone-100 dark:text-stone-300 dark:hover:bg-stone-900",
          ]}
        >
          {link.label}
        </a>
      );
    })
  }
</nav>
