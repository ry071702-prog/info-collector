---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { allArticles } from "../../lib/articles";
import { formatJapaneseDate } from "../../lib/japaneseDate";

const base = import.meta.env.BASE_URL.replace(/\/$/, "");

// 直近30日のうち記事のある日付を新しい順に
const articles = allArticles();
const dateMap = new Map<string, number>();
for (const a of articles) {
  const ts = new Date(a.timestamp);
  if (Number.isNaN(ts.getTime())) continue;
  const jst = new Date(ts.getTime() + 9 * 60 * 60 * 1000);
  const d = jst.toISOString().slice(0, 10);
  dateMap.set(d, (dateMap.get(d) ?? 0) + 1);
}
const dates = [...dateMap.entries()]
  .sort((a, b) => b[0].localeCompare(a[0]))
  .slice(0, 30);
---

<BaseLayout title="新聞アーカイブ">
  <section class="mb-6">
    <h1 class="section-title text-3xl sm:text-4xl">新聞アーカイブ</h1>
    <p class="mt-2 text-sm section-copy">
      毎日その日のニュースを 1 ページの新聞風レイアウトでまとめます。見出しから元記事へ遷移できます。
    </p>
  </section>

  {dates.length === 0 ? (
    <p class="glass-empty p-6 text-sm">まだ新聞が発行されていません。</p>
  ) : (
    <ul class="glass-panel divide-y divide-white/45 overflow-hidden dark:divide-white/10">
      {dates.map(([date, count]) => (
        <li>
          <a href={`${base}/newspaper/${date}/`} class="group flex items-center justify-between gap-4 px-5 py-4 transition hover:bg-white/42 dark:hover:bg-white/8">
            <div>
              <h2 class="text-lg font-black text-[var(--text-primary)]">
                {formatJapaneseDate(`${date}T12:00:00+09:00`)}
              </h2>
              <p class="text-xs section-kicker">{count} 件の記事を収録</p>
            </div>
            <span class="text-xs font-bold section-kicker group-hover:text-[var(--text-primary)]">読む</span>
          </a>
        </li>
      ))}
    </ul>
  )}
</BaseLayout>
